{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Project",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/castlemania_interface.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build-and-collect",
            "MIMode": "gdb",
            "externalConsole": true
        }
    ]
}